<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.senai_pdm_2021_exemplo1">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Senai_pdm_2021_exemplo1">
        <activity android:name=".SplashIntro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActionsBook"
            android:label="Edicoes do livro"
            android:parentActivityName=".ReadBook" />
        <activity android:name=".ReadBook"
            android:label="Livros disponíveis"/>
        <activity
            android:name=".RegisterBook"
            android:label="Register book"
            android:parentActivityName=".ReadBook" />
    </application>

</manifest>